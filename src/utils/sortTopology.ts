// NOTE: We implement sort within client instead of requesting th API cause the topology is not paged
// TODO: Implement the function
export function getSortedTopology(topology, sortBy, sortByType) {
  return topology;
}
